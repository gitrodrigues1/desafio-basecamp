spring:
    datasource:
        url: jdbc:postgresql://localhost:15432/basecamp_db
        username: pgdb
        password: pgPW2023!
    jpa:
        show-sql: true
        defer-datasource-initialization: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true

